# 秋招+考公助手（个人自用版）产品需求文档（PRD）

> 版本：v2.0  日期：2026-07-30  状态：草案
> 定位变更：从"面向用户的产品"调整为"个人自用工具"，砍掉多用户、合规、商业化相关内容，强调轻量、省钱、实用。

---

## 一、产品概述

### 1.1 背景
秋招 + 考公考编双线并行，信息分散在牛客、应届生网、各省人事考试网、公司官网等多处，难以统一跟踪；简历需要针对不同岗位反复调整；投递/报名/笔试/面试多线进行容易遗漏。

### 1.2 产品定位
一个**个人自用的求职/考公信息管理 + AI 辅助工具**，整合"信息采集—智能匹配—简历优化—投递/报名跟踪—面试/笔试管理"全流程。

### 1.3 使用场景
- 单用户（自己用），本地或自部署
- 数据量小（几百到几千条岗位），不需要高并发
- 优先省钱、省事，能用免费方案就不花钱

### 1.4 设计原则
- 轻量：SQLite + 单机部署即可
- 省钱：能用免费额度就不付费，大模型用最便宜的
- 可切换：大模型、数据源都要能方便更换
- 够用就行：不追求产品级体验，自己用着顺手即可

---

## 二、核心功能模块

### 模块一：岗位信息中心（秋招 + 考公考编）

**目标**：自动 + 手动聚合两类岗位信息。

#### 1.1 秋招岗位

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 岗位列表 | 岗位名称、公司、地点、薪资、投递起止时间、来源 | P0 |
| 岗位详情 | 岗位描述、要求、福利、内推信息 | P0 |
| 搜索/筛选 | 按公司、地点、岗位类型、薪资、投递状态筛选 | P0 |
| 岗位收藏 | 收藏感兴趣的岗位 | P0 |
| 岗位对比 | 多个岗位横向对比 | P1 |
| 公司信息 | 公司规模、行业、口碑（可手动备注） | P2 |
| 内推码记录 | 记录内推码与内推人 | P2 |

#### 1.2 考公考编岗位

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 公告列表 | 国考/省考/事业编/选调生公告，含报名起止时间 | P0 |
| 职位表解析 | 上传/爬取职位表（Excel），解析为结构化数据 | P0 |
| 职位筛选 | 按学历、专业、政治面貌、基层经验、地点筛选 | P0 |
| 报名时间跟踪 | 报名开放/截止时间 + 缴费时间 + 准考证打印时间 | P0 |
| 职位收藏 | 收藏感兴趣的职位 | P0 |
| 报名人数查询 | 部分省份可查报名人数（辅助判断竞争比） | P1 |
| 考试日程 | 笔试/面试时间统一进日历 | P0 |

**考公考编数据源**
- 国家公务员局（国考）：http://bm.scs.gov.cn/
- 各省人事考试网（省考/事业编）
- 公考雷达/粉笔等聚合站（可爬，注意频率）
- 职位表通常以 Excel 附件发布，下载后解析

---

### 模块二：数据自动采集

**目标**：定时自动抓取岗位信息，减少手动维护。

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 定时爬虫 | 每天凌晨自动爬取各数据源 | P0 |
| 多数据源 | 牛客校招日历、应届生求职网、各省人事考试网等 | P0 |
| 去重 | 同一岗位多源出现时自动去重 | P0 |
| 增量更新 | 只抓新增/变更，不全量重抓 | P0 |
| 爬虫规则配置 | 每个数据源一套解析规则，可单独启停 | P1 |
| 手动触发 | 支持手动点"立即抓取" | P1 |
| 抓取日志 | 记录每次抓取的成功/失败/数量 | P1 |

**数据源优先级（秋招）**
1. 牛客网校招日历（结构统一，首选）
2. 应届生求职网（信息全，页面老好爬）
3. 重点目标公司官网校招页（手动配规则）
4. 微信公众号历史文章（"校招薪水"等，可选）

**数据源优先级（考公考编）**
1. 国家公务员局官网（国考）
2. 目标省份人事考试网（省考/事业编）
3. 公考雷达（聚合，辅助）

**技术实现**
- Python + requests/Playwright
- APScheduler 定时任务（每天 02:00 跑）
- 自用不怕封，但也要加随机延迟和 User-Agent，别太暴力
- 存 SQLite

---

### 模块三：简历与求职画像

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 简历上传与解析 | PDF/Word 上传，AI 解析为结构化数据 | P0 |
| 求职偏好设置 | 期望地区、岗位类型、行业、薪资、公司规模 | P0 |
| 考公偏好设置 | 目标省份、学历、专业、政治面貌、基层经验 | P0 |
| 简历多版本 | 针对不同岗位/方向维护多份简历 | P1 |
| 简历在线编辑 | 可视化编辑，导出 PDF | P2 |
| 画像标签化 | 自动提取技能、项目、学历标签 | P1 |

---

### 模块四：智能匹配与推荐

**目标**：基于画像与岗位库，推荐适配岗位并解释原因。

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 每日推荐 | 每日推送适配的秋招岗位 + 考公职位 | P0 |
| 匹配度评分 | 0-100 分 | P0 |
| 适配原因 | 列出匹配点（技能/学历/地区/专业） | P0 |
| 优缺点分析 | 优势项与差距项 | P0 |
| 所需技能清单 | 岗位要求 vs 现状，标注已具备/待补齐 | P0 |
| 考公职位匹配 | 按学历/专业/政治面貌/基层经验硬过滤 + AI 解释 | P0 |
| 不适配说明 | 解释为何不推荐 | P1 |

**匹配思路**
- 秋招：规则层硬过滤（学历/地点）+ AI 语义匹配 + 原因生成
- 考公：规则层硬过滤（学历/专业/政治面貌/基层经验，这些是硬门槛）+ AI 辅助解释竞争难度

---

### 模块五：日历与推送

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 求职日历 | 投递 DDL、报名 DDL、笔试、面试统一日历 | P0 |
| 每日推送 | 今日可投/即将截止/今日笔试面试 | P0 |
| DDL 倒计时 | 截止倒计时，提前 3/1/0 天提醒 | P0 |
| 系统日历集成 | 同步到系统日历（macOS/手机） | P1 |
| 微信/邮件提醒 | 可选通道 | P2 |

---

### 模块六：简历优化与内容生成

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 简历针对性优化 | 输入岗位，AI 给逐条修改建议 | P0 |
| 自我介绍生成 | 根据岗位与简历生成自我介绍（多版本） | P0 |
| 求职信/邮件生成 | 生成投递邮件模板 | P1 |
| 面试回答准备 | 预测高频问题并生成参考回答 | P1 |
| 项目经历润色 | STAR 法则优化 | P1 |
| 公考申论/面试准备 | AI 辅助申论练习、结构化面试模拟 | P2 |

---

### 模块七：投递/报名与流程跟踪

**目标**：秋招投递 + 考公报名统一管理。

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 投递记录 | 秋招岗位投递记录（岗位/时间/渠道/简历版本） | P0 |
| 报名记录 | 考公考编报名记录（职位/报名时间/缴费状态） | P0 |
| 流程状态 | 秋招状态机 + 考公状态机 | P0 |
| 进度看板 | 看板视图按状态分组 | P0 |
| 面试/笔试日程 | 时间、形式、地点、轮次 | P0 |
| 面试反馈记录 | 每轮复盘 | P1 |
| 时间线视图 | 单条记录的完整时间线 | P1 |

**秋招状态机**
```
待投递 → 已投递 → 简历初筛 → 笔试 → 一面 → 二面 → HR面 → offer → 接受/拒绝
                                                    ↘ 拒绝（任意阶段）
```

**考公状态机**
```
待报名 → 已报名 → 已缴费 → 准考证打印 → 笔试 → 成绩公布 → 面试 → 体检政审 → 录用
                                                        ↘ 未进面/放弃
```

---

### 模块八：面试与题库

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 面经聚合 | 按公司/岗位聚合面经，AI 总结高频问题 | P1 |
| AI 模拟面试 | 针对目标岗位模拟面试 | P2 |
| 公考题库 | 行测/申论练习 + 错题本 | P2 |
| 面试日历 | 面试安排单独视图 | P1 |

---

## 三、用户主流程

### 流程一：首次使用
```
启动 → 上传简历 → 设置秋招偏好 + 考公偏好 → AI 解析画像 → 触发首次爬取 → 进入首页
```

### 流程二：秋招投递
```
每日推荐/搜索 → 查看匹配分析 → 收藏/投递 → 选简历版本 → AI 优化简历 → 记录投递 → 进日历
```

### 流程三：考公报名
```
公告/职位表 → 职位筛选（硬条件过滤）→ 查看匹配职位 → 收藏 → 报名 → 记录报名 → 进日历
```

### 流程四：笔试/面试准备
```
收到通知 → 录入日程 → 查看面经/题库 → AI 生成自我介绍/答题准备 → 模拟面试 → 参加面试 → 复盘
```

---

## 四、信息架构（首页 Tab）

```
首页（每日推荐 + 日历）
├── 今日待办（可投岗位/即将截止/今日笔试面试）
├── 每日推荐（秋招 + 考公分区）
└── 日历视图

岗位（信息中心）
├── 秋招 / 考公考编 分区
├── 推荐 / 收藏 / 全部
└── 搜索 + 筛选

投递（流程跟踪）
├── 秋招看板 / 考公看板
├── 时间线视图
└── 日历视图

我的
├── 简历管理（多版本）
├── 求职偏好 + 考公偏好
├── 数据源管理（爬虫开关/日志）
├── 大模型设置（切换模型/填 API Key）
└── 面经与题库
```

---

## 五、技术方案（个人自用轻量版）

### 5.1 整体架构
```
前端（Web 单页 / 桌面端）
        ↓
   后端 API（FastAPI / Flask）
        ↓
业务逻辑 + AI 抽象层 + 爬虫调度
        ↓
SQLite + 本地文件（简历）+ LLM API
```

### 5.2 推荐技术选型

| 模块 | 选型 | 理由 |
|------|------|------|
| 后端 | Python + FastAPI | 爬虫生态好，AI 库齐全，自用够快 |
| 数据库 | SQLite | 单文件，零运维，自用足够 |
| 前端 | 方案 A：HTML+JS 单页（最轻）<br>方案 B：Streamlit（最快出原型）<br>方案 C：Flutter Desktop | 自用推荐 Streamlit 先跑起来，顺眼了再换 |
| 爬虫 | requests + BeautifulSoup + Playwright（动态页） | 简单够用 |
| 定时任务 | APScheduler | Python 内嵌，不用额外装 |
| 大模型 | DeepSeek API（默认）+ 抽象层支持切换 | 最便宜，兼容 OpenAI 格式 |
| 推送 | macOS 本地通知 + 系统日历集成 | 自用够用 |
| 部署 | 本机直接跑 / Docker | 不用上云 |

### 5.3 大模型抽象层设计

```python
# 统一接口
class LLMClient:
    def chat(self, messages: list[dict]) -> str: ...

# 各实现都兼容 OpenAI 格式，换模型只改配置
class DeepSeekClient(LLMClient): ...   # 默认，最便宜
class QwenClient(LLMClient): ...       # 阿里通义，有免费额度
class OpenAIClient(LLMClient): ...     # 贵但强
class OllamaClient(LLMClient): ...     # 本地免费

# 配置切换
# config.yaml
# llm:
#   provider: deepseek
#   base_url: https://api.deepseek.com
#   api_key: xxx
#   model: deepseek-chat
```

**成本估算（自用）**
- DeepSeek：约 ¥1-2/百万 token，一个月估计几块到几十块
- 可先用各平台新用户免费额度白嫖几个月
- 有显卡可上 Ollama 本地模型，完全免费

### 5.4 爬虫设计

```
spiders/
├── base.py          # 爬虫基类
├── nowcoder.py      # 牛客校招日历
├── yingjiesheng.py  # 应届生求职网
├── guokao.py        # 国考公告
├── shengkao.py      # 省考（按省配置）
└── company_xxx.py   # 重点公司官网（手动配）

scheduler.py         # APScheduler 定时调度
parser.py            # 解析规则统一入口
```

- 每天凌晨 02:00 自动跑
- 每个源独立，失败不影响其他
- 加随机延迟（1-5 秒）+ User-Agent 轮换
- 增量抓取，按发布时间/ID 去重

---

## 六、非功能性需求

| 维度 | 要求 |
|------|------|
| 性能 | 自己用，响应 < 3s 即可 |
| 隐私 | 简历本地存储，API 调用不传敏感信息给第三方模型时注意脱敏 |
| 部署 | 本机运行，开机自启 |
| 备份 | SQLite 文件定期备份 |

---

## 七、MVP 范围与迭代规划

### MVP（v1.0）— 先跑起来
聚焦"信息 + 匹配 + 跟踪"闭环：
- 爬虫：牛客校招日历 + 国考公告（2 个源先跑通）
- 简历上传解析 + 求职/考公偏好
- 每日推荐 + 匹配分析 + 优缺点 + 所需技能
- 日历 + DDL 倒计时 + 本地通知
- 简历优化建议 + 自我介绍生成
- 投递/报名记录 + 流程状态 + 看板
- 前端：Streamlit 先出原型

### v1.1
- 加省考/事业编爬虫源
- 岗位对比、内推码记录
- 面试日程 + 面试反馈
- 系统日历集成

### v2.0
- 面经聚合 + AI 总结
- AI 模拟面试
- 公考题库
- 换成 Flutter Desktop 或 Web 前端，体验更好

---

## 八、风险与应对

| 风险 | 应对 |
|------|------|
| 爬虫被反爬 | 自用量小，加延迟基本没事；必要时手动录入 |
| 公司校招页结构多变 | 重点公司单独配规则，长尾靠聚合站 |
| AI 生成质量不稳定 | 规则 + AI 混合，结果可人工修正 |
| 大模型 API 涨价/停服 | 抽象层支持切换，随时换 |
| 考公职位表格式不一 | Excel 解析做容错，异常的手动处理 |
| 秋招时间紧 | MVP 砍到最小，先跑通核心闭环 |

---

## 九、待确认事项

1. 前端用 Streamlit 快速出原型，还是直接上 Web/Flutter？（建议先 Streamlit）
2. 本机跑还是放个小服务器？（本机最省事）
3. 大模型先用 DeepSeek 还是白嫖各家免费额度？
4. 考公目标省份是哪些？（决定先写哪几个省考爬虫）
5. 是否需要手机端查看？（决定要不要做 Web + 手机访问）
